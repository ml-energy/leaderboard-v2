#!/bin/bash
### Mistral-7b

############################## GPU 0 ##############################
# vllm
docker run --gpus device=0 --shm-size 1g -p 8000:8000 \
        --runtime nvidia \
        --network benchmark-net \
        -e HUGGING_FACE_HUB_TOKEN=$token \
        -v ~/.cache/huggingface:/root/.cache/huggingface \
        --name vllm0_mistral \
        -d vllm/vllm-openai:latest \
        --tensor-parallel-size 1 \
        --model mistralai/Mistral-7B-Instruct-v0.2

# Benchmark
docker run \
        --gpus device=0 \
        --name mistral7_25 \
        --network benchmark-net \
        -v /home/ohjun/leaderboard/sharegpt:/data \
        -v /home/ohjun/leaderboard/scripts/vllm-script/results:/results \
        -d benchmark:latest \
        --backend vllm \
        --dataset /data/ShareGPT_V3_filtered_1000.json \
        --host vllm0_mistral \
        --port 8000 \
        --model mistralai/Misbenchmarktral-7B-Instruct-v0.2 \
        --out-filename /results/vllm-mistral-7b-rr025 \
        --request-rate 0.25 \
        --num-runs 1


############################## GPU 1 ##############################
# vllm
docker run --gpus device=1 --shm-size 1g -p 8001:8000 \
        --runtime nvidia \
        --network benchmark-net \
        -e HUGGING_FACE_HUB_TOKEN=$token \
        -v ~/.cache/huggingface:/root/.cache/huggingface \
        --name vllm1_mistral \
        -d vllm/vllm-openai:latest \
        --tensor-parallel-size 1 \
        --model mistralai/Mistral-7B-Instruct-v0.2

# Benchmark
docker run \
        --gpus device=1 \
        --name mistral7_50 \
        --network benchmark-net \
        -v /home/ohjun/leaderboard/sharegpt:/data \
        -v /home/ohjun/leaderboard/scripts/vllm-script/results:/results \
        -d benchmark:latest \
        --backend vllm \
        --dataset /data/ShareGPT_V3_filtered_1000.json \
        --host vllm1_mistral \
        --port 8000 \
        --model mistralai/Misbenchmarktral-7B-Instruct-v0.2 \
        --out-filename /results/vllm-mistral-7b-rr050 \
        --request-rate 0.5 \
        --num-runs 1


############################## GPU 2 ##############################
# vllm
docker run --gpus device=2 --shm-size 1g -p 8002:8000 \
        --runtime nvidia \
        --network benchmark-net \
        -e HUGGING_FACE_HUB_TOKEN=$token \
        -v ~/.cache/huggingface:/root/.cache/huggingface \
        --name vllm2_mistral \
        -d vllm/vllm-openai:latest \
        --tensor-parallel-size 1 \
        --model mistralai/Mistral-7B-Instruct-v0.2

# Benchmark
docker run \
        --gpus device=2 \
        --name mistral7_75 \
        --network benchmark-net \
        -v /home/ohjun/leaderboard/sharegpt:/data \
        -v /home/ohjun/leaderboard/scripts/vllm-script/results:/results \
        -d benchmark:latest \
        --backend vllm \
        --dataset /data/ShareGPT_V3_filtered_1000.json \
        --host vllm2_mistral \
        --port 8000 \
        --model mistralai/Misbenchmarktral-7B-Instruct-v0.2 \
        --out-filename /results/vllm-mistral-7b-rr075 \
        --request-rate 0.75 \
        --num-runs 1


############################## GPU 3 ##############################
# vllm
docker run --gpus device=3 --shm-size 1g -p 8003:8000 \
        --runtime nvidia \
        --network benchmark-net \
        -e HUGGING_FACE_HUB_TOKEN=$token \
        -v ~/.cache/huggingface:/root/.cache/huggingface \
        --name vllm3_mistral \
        -d vllm/vllm-openai:latest \
        --tensor-parallel-size 1 \
        --model mistralai/Mistral-7B-Instruct-v0.2

# Benchmark
docker run \
        --gpus device=3 \
        --name mistral7_1 \
        --network benchmark-net \
        -v /home/ohjun/leaderboard/sharegpt:/data \
        -v /home/ohjun/leaderboard/scripts/vllm-script/results:/results \
        -d benchmark:latest \
        --backend vllm \
        --dataset /data/ShareGPT_V3_filtered_1000.json \
        --host vllm3_mistral \
        --port 8000 \
        --model mistralai/Misbenchmarktral-7B-Instruct-v0.2 \
        --out-filename /results/vllm-mistral-7b-rr100 \
        --request-rate 1 \
        --num-runs 1
